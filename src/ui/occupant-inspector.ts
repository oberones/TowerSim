import type { OccupantInspection } from '../app/game/occupant-queries';
import { element } from './elements';
/** Reuse one textual inspector while a selected identity walks, sleeps and leaves. */
export function createOccupantInspector(){const node=element('section'),text=element('p','Click a walking person to inspect them.');node.append(element('h2','Person'),text);return {node,update:(p:OccupantInspection|null)=>{text.textContent=p?`${p.id} · ${p.kind} · ${p.state} · Goal: ${p.goal.kind==='office'?p.goal.facilityId:p.goal.kind}${p.schedule?` · Visit: ${p.schedule.status}`:''}${p.trip?` · Walking ${p.trip.elapsed.walking}s · Stairs ${p.trip.elapsed.stair}s · Waiting ${p.trip.elapsed.waiting}s · Riding ${p.trip.elapsed.riding}s · Stranded ${p.trip.elapsed.stranded}s · Denials ${p.trip.denials} · Transfers ${p.trip.transfers} · Trip ${p.trip.purpose} (${p.trip.outcome})`:''}`:'Click a walking person to inspect them.';}};}
