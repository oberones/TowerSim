# Phase 5: offices and a real walking workday

Run `npm test`, `npm run build`, `npm run build:browser-test`, and `npm run check:browser-builds`. Serve release output with `npm run preview -- --host 127.0.0.1 --port 4173`; use the actual port printed by Vite if occupied. Serve the separate fixture build with `npm run preview:browser-test -- --host 127.0.0.1 --port 4174`.

Choose Office, enter **Office floor** and **Office start cell**, then **Place office**. The same validator supplies preview and commit. Default offices occupy 16 cells, cost $600, retain the shared hallway, and require already constructed space. Placement alone adds neither people nor rent. Floor demolition protects room footprints and committed walking support. Select an office on Canvas with Inspect to see tenancy, assigned workers, physical presence, access, daily rates, accrued amounts, recent rent and finite market availability. **Remove selected office** is free with no refund, and separately settles accrued rent and costs.

The first positive advance executes the pending 06:00 review at the original tick. Accessible offices lease whole 32-worker workforces in stable order, subject to the finite market. Later construction waits until the next day's review. Workers receive seeded arrival/departure goals within the disclosed windows. They enter at the lobby, walk for nonzero time, become present only on physical admission, sleep inside until departure, and walk back to the lobby. Click a visible worker to retain its identity/type/goal/state inspector while paused, inside or dormant outside. Failed visits and real exit journeys retain separate metrics. Rent depends on tenancy and access, not attendance; operating costs continue while inaccessible.

Demolition cancels unstarted visits, preserves valid current legs, and sends indoor workers out from the surviving entrance. A disconnected exit remains stranded until construction restores a route. Former workers retire after real exit or outside cancellation; ordinary leased workers retain identity. The terminated market allocation is released only at the next review. Midnight has one source-settlement owner, uses exact prorating rounded once, and cannot bill a deleted source again.

The fixture chooser offers **One worker — same-floor office** (default walking speed, one worker) and **One worker — visible approach** (one worker at the default pace, prepared two simulated minutes before arrival). Both use validated scenario data and ordinary construction/leasing/advancement before mount. After mounting only ordinary controls operate the game. Neither fixture is bundled into release output. Default release workforce remains 32. Walking now takes 30 simulated seconds per cell; a 28-cell lobby-to-office walk takes 7 real seconds at Normal, 1.75 seconds at 4×, and 0.875 seconds at 8×. The game day still takes 12 real minutes at Normal. People use larger outlined markers with a wider click target, including when zoomed out. This changes actual travel duration and measured trip time, so the displayed person stays at the true simulation position. Start a new tower to use the new scenario default; existing snapshots retain their saved walking pace.

State rules/content identifiers are `tower-offices-v1` / `mvp-offices-v1`; incompatible earlier construction snapshots are rejected. The pure codec now checks office reservations, leases, physical state, schedules, wakeups, source intervals and trip references. Walk endpoints plus saved start/duration encode exact progress; rebuild consumes no RNG. The application runner coalesces intervals with no transition while preserving every integer-time outcome. Later movement modes must add their own due transitions; they cannot rely on per-frame updates.

Transport, restaurants, aggregate traffic scores, the financial reporting panel, progression and browser storage remain their later phases. Phase 5 browser observations and outstanding checks are recorded in [one-walker evidence](../tests/browser/evidence/us03-one-walker.md) and [office evidence](../tests/browser/evidence/us03-offices.md).
