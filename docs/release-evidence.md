# Release evidence procedure

For each release, review the source diff and asset inventory against docs/provenance.md. Record author/source, permission and modifications for each addition. Exclude proprietary assets and copied game text. Review the lockfile diff, installed package license files, and generated bundle helpers; investigate missing or incompatible license declarations before redistribution.

Run the checked release build, tests, benchmark workload appropriate to the slice, and browser-build isolation checker. Inspect static output for unexpected scripts, remote resources and fixture code. Record SHA-256 hashes, commit plus dirty diff identity, versions and unmet checks using tests/browser/evidence/template.md. Preserve the output and measurements associated with that identity.

Actual Chrome, Firefox and Safari observations, storage/reopen checks, sustained performance and new-player evaluation remain separate gates defined in specs/001-playable-tower-mvp/validation.md. Do not mark these complete from Node tests or a prepared harness.

## Phase 1 evidence (2026-09-10)

Node 24.20.0: offline lockfile reinstall (`npm ci --offline`), strict type checks, domain boundary checker with 21 positive/negative fixtures, two unit/integration setup tests, separate integration command, benchmark runner self-check (3 warmups/10 restored trials), release and browser-test production builds, and shared-graph/output-isolation check passed. The benchmark only exercises the harness; it establishes no simulation throughput. No gameplay, native-browser, visual, storage, player or performance qualification is claimed.

## Phase 2 evidence (2026-09-10)

T006–T015 were implemented with tests written and observed failing before their implementation. The final suite has 30 passing tests across 11 files, including three seeds across three days, segmented/reconstructed continuation, pure previews, rejected-command sequencing, exact money and overflow nonmutation, strict state validation, scheduler cancellation and rollback boundaries. Two additional review regressions were observed failing and then fixed: sparse arrays masked by named properties, and a reschedule counter behind prior sequence allocation.

Type and domain-boundary checks, separate integration entrypoint, release/browser-test production builds and shared-build isolation pass. Benchmarks run 3 warmups plus 10 restored trials with matching digests; full methodology/results are in [performance/kernel.md](performance/kernel.md). The C reference was compiled and its ten outputs matched the TypeScript golden vector. There is still no gameplay or native-browser qualification in this headless phase.

## Phase 3/4 evidence (2026-09-10)

Implemented T016–T022 and T024–T029: immutable scenario/content, configurable initial tower, application-owned pacing/session lifecycle, Canvas camera/cache, named controls, floor construction/demolition, explicit quotes, floor inspection and strict pure state continuation. The remaining phase checkpoints T023/C0 and T030/M1 are **not fully qualified** because actual stable Chrome/Firefox/Safari observations could not be completed. Phase 5 onward remains untouched.

Final checks: 62 tests / 23 files pass; separate integration entrypoint passes 21 tests / 7 files; strict types, the domain checker and its 21 regression fixtures pass; descriptive comments are checked for 166 named production functions/methods; release and browser-test builds plus graph/output isolation pass. Both new construction benchmarks pass, using three warmups and ten restored trials with matching authoritative digests. See [performance/construction.md](performance/construction.md) and its retained JSON for measured batch timings and limits.

Supplemental in-app browser observations cover the paused lobby/site, all displayed speed modes with advancing time, pause, New Game cancel/discard, two upper floors plus extension, normalized span inspection, unsupported quotes, unchanged quotes after zoom, safe free demolition and upper-support rejection. A real observed RAF timestamp-ordering error was corrected and regression-tested before the final smoke. Exact observations and native-browser gaps are in [C0 evidence](../tests/browser/evidence/us01-start.md) and [M1 evidence](../tests/browser/evidence/us02-construct-tower.md). Source/artifact hashes are retained in [phase-3-4-artifacts.json](phase-3-4-artifacts.json). No native storage, final performance, user evaluation, deployment or complete-MVP qualification is claimed.


## Phase 5 evidence (2026-09-10)

Implemented office placement/leasing, finite workforce schedules, real walking/admission/exit, retained occupant inspection, exact source accrual/settlement, safe demolition, former-worker retirement and strict office save-state continuation. Implementation tasks T031–T041 and T043–T046 are complete. T042/T047 remain open for full browser qualification; the native Firefox release smoke and supplemental in-app observations are explicitly scoped in [office evidence](../tests/browser/evidence/us03-offices.md) and [M2 evidence](../tests/browser/evidence/us03-one-walker.md).

New functions have descriptive comments enforced by the build. Domain-boundary regression fixtures now allow literal occupant location properties while continuing to reject browser location access. The privately owned application runner validates/rebuilds once; public advancement still validates external input. Phase 5 tests cover normal and abandoned journeys, physical presence, rent/access, exact rounding, safe removal and repair, market release, no repeated schedule draws, immutable inspectors, malformed saves and next-day continuation. All 12 restored-trial benchmarks pass; [walking measurements](performance/occupants.md) distinguish transition-free domain timing from unmeasured rush/browser performance. Final verification counts and source/artifact hashes are recorded in [phase-5-artifacts.json](phase-5-artifacts.json).


## Phase 6/7 evidence (2026-09-10)

Implemented T048–T052 and T054–T063: adjacent stairs, physical traversal and topology recovery; standard elevator construction, separate service/stop/car identities, fixed collective sweep, timed car phases and transfers, physical queue requests, semantic route-mode preference, immutable inspection and ordinary construction/service controls. The prior office-only snapshot rules are superseded by explicitly validated transport rules. Function comments remain build-enforced.

Final verification passes 168 tests across 59 files, strict types, 24 boundary regression fixtures, comments for 299 named production functions/methods, both production builds and build isolation. Twelve observation-fixture regressions cover stair boundaries and all nine elevator phases. Current Node/toolchain versions and static-build/source hashes are in [phase-6-7-native-artifacts.json](phase-6-7-native-artifacts.json); [phase-6-7-artifacts.json](phase-6-7-artifacts.json) preserves the earlier run. The focused [single-elevator benchmark](performance/single-elevator.md) passed with 13 identical restored-run digests. It does not qualify browser performance or multi-passenger congestion.

After the Mac was unlocked, native Firefox 155.0.1 and Safari 26.5 exercised stair ascent/descent, occupied-removal rejection, stranded-exit repair, tracked lobby-to-office elevator journeys, every paused car phase, loaded guards and ordinary release construction smoke. Exact observation methods and limits are recorded in [stair evidence](../tests/browser/evidence/us04-stairs.md) and [elevator evidence](../tests/browser/evidence/us05-one-elevator-rider.md). T053/C1 and T064/M3 remain unchecked because Chrome is deferred at the user's request. No phase 8 or later task is advanced.


## Phase 8–10 evidence (2026-09-10)

Implemented T065–T071, T073–T078 and T080–T086: indexed FIFO membership and waiting-time aggregates, multi-person exchange/denials, physical queue rendering, recurring-workforce traces and overnight guards, exact score arithmetic, full-population morning reports, current-day accumulation, bounded completed-day summaries, read-only traffic views and person inspection. First boarding no longer counts as an elevator transfer; the existing single-rider assertions were corrected to the documented rule.

Final validation on pinned Node 24.20.0: **200 tests across 76 files pass**, strict domain/browser/test types, 24 boundary fixtures, descriptive comments on 335 named production functions/methods, release and prepared-fixture production builds, and graph/output isolation. New behavior was tested first; pre-existing exchange/workday behavior was extended with passing behavioral coverage. The new FIFO module and score/report/query APIs were observed failing before implementation, and the first-boarding transfer assertion failed before correction.

Tests cover nine-person capacity, multi-unload cursor stability, frozen cutoff/late arrivals, full-car intermediate stops, repeated denials, selected-goal cancellation, retained trip histories, default 32-worker attendance/return travel, overdue committed riders, retirement, all speed/callback schedules with timed edits, zero-wait denominators, failed/unresolved reporting, midnight ownership, malformed report rejection and 35-day retention. Queue/service and report snapshots resume identically into subsequent workdays.

Three warmups plus ten restored trials per workload retain matching digests. See [queue measurements](performance/queues.md), [office rush and route measurements](performance/office-rush.md), and [report measurements](performance/transport-reports.md). These are focused headless measurements, not browser frame-rate or final-scale qualification. Initial exploratory runs used the shell's Node 20; final evidence was rerun on Node 24.20.0.

Native Firefox 155.0.1 and Safari 26.5 observations and their limits are in [M4 queue evidence](../tests/browser/evidence/us07-elevator-queue.md), [C2 workforce evidence](../tests/browser/evidence/us06-office-workday.md), and [C3 report evidence](../tests/browser/evidence/us11-traffic-inspection.md). Both browsers completed ordinary release smoke: empty reports, office placement, first leasing, real clock advancement and pause. Source/output hashes are in [phase-8-10-artifacts.json](phase-8-10-artifacts.json). **T072/T079/T087 remain unchecked**: Chrome is unavailable in the enabled inventory, and full repeated-rush/overnight/manual checkpoint traces remain to be collected. No later congestion, restaurant, native-storage, final-scale or player-evaluation gate is claimed.

## Phases 11–13 — 2026-09-11

Implemented T088–T091, T093–T099 and T101–T106: canonical congestion, real added-capacity routing and waiter migration, continuous-play improvement, safe service/demolition reconciliation and actionable access diagnostics. T092/T100/T107 remain open for complete native-browser qualification. Phase 14 was not started.

Final pinned Node 24.20.0 validation: **226 unit/integration tests in 87 files pass**; strict types; 24 architectural regression fixtures; descriptive comments verified for 342 named production functions/methods; successful release and prepared-fixture production builds; shared graph/settings and release isolation. `npm run check:release` runs the mandatory unfiltered tests before both checked builds. The full benchmark suite passes **18 tests in 9 files** with three warmups/ten restored trials and digest agreement. The dormant benchmark now explicitly uses its wider synthetic schedule window and keeps retained workforce traces synchronized with its controlled sleeping-worker arrangement.

The congestion gate retains all 96 workers, schedules, tenants and current RNG, and passes normal/4x/8x plus segmented/pause replay. Shaft construction legitimately brings lazy office accrual counters current at the edit tick; the comparison checks unchanged tenant/geometry/demand data while retaining equivalent accrual semantics. [Acceptance results](acceptance/congestion.md) contain exact numbers and fixture hash; [transport edit measurement](performance/transport-edits.md) records the 96-person edit latency without claiming final browser-scale performance.

Firefox observed the full baseline recovery, built shaft B through ordinary controls and displayed better next-day cohort results in that same continuing tower. Firefox and Safari loaded the release site. Exact native observations and remaining gaps are in the phase-specific browser evidence; Chrome is unavailable. [Artifact hashes](phase-11-13-artifacts.json) identify both outputs. No restaurant, Save/Load, final-scale performance, deployment or new-player evaluation is claimed.

## Phases 14–16 — 2026-09-11

Implemented T108–T112, T114–T119 and T121–T124: bounded reconcilable financial history, operating summaries and readable receipts; real restaurant construction/admission/visits/exit; once-only payment and demolition liabilities; daily finite meal allocation; shared worker/customer transport; retirement and exact continuation. All new named production functions have descriptive comments. The implementation stops before phase 17; Level 2 and native Save/Load remain later phases.

Final validation uses pinned **Node 24.20.0**: **276 tests in 101 files**, strict domain/browser/test types, 24 architectural regression fixtures, descriptive comments on 375 named production functions/methods, both production builds and graph/settings/output isolation. The mandatory congestion suite retains exactly the earlier numerical baseline/improvement and full-cohort assertions at normal/4×/8× with pauses. Its canonical fixture hash changed only to reflect the new required persistent fields, source labels and explicit `tower-restaurant-v1` / `mvp-restaurant-v1` compatibility boundary. Earlier transport-only snapshots reject rather than receive invented data.

Tests include initial/next-day reviews, inaccessible and demolished destinations, stair preference, elevator rides, mixed four-worker/five-customer eight-seat exchange, admission-only payments, nonzero admission-relative visits, cancellation, supported exits, protected stranding, archive boundaries, safe integer overflow and ledger reconciliation. Three explicit seeds across three days, actual application pacing/pauses, occupied restoration and 35 days of finite demand cover stable leased-worker identity, bounded closed histories, no accumulating departed customers and exact revenues.

The focused benchmark suite passes two tests containing three measured workloads (three warmups/ten measured trials each with identical final digests). [Finance history](performance/finance-history.md) and [restaurant demand](performance/restaurant-demand.md) retain Node 24 JSON and methodology. Earlier exploratory Node 20 runs are superseded. Preparing the 35-day finance benchmark takes approximately a minute and is outside its query timing; its timeout was explicitly enlarged to 120 seconds. No final reference-tower performance budget is claimed.

Scoped native Safari 26.6.2 observed physical incoming/admission/forced exit, one payment, accrued removal, reconciliation, unaffordable placement and usable negative-cash play. Firefox 155.0.1 observed a meal wave, the 20/20 allocation and ordinary construction of a third room without same-day demand multiplication. **T113/T120/T125 remain unchecked** for the complete three-browser C5/C6 exercises; Chrome is unavailable in the enabled control inventory and several full-checkpoint observations remain unperformed. Exact scope is in [C5](../tests/browser/evidence/us14-finances.md), [one-customer C6](../tests/browser/evidence/us12-restaurant-visit.md) and [meal C6](../tests/browser/evidence/us13-meal-demand.md). These observations used the separate prepared build; they do not qualify release deployment, browser storage, final-scale budgets or new-player outcomes.

See [phase guide](restaurants-and-finances.md) for runnable controls/fixtures and [artifact identity](phase-14-16-artifacts.json) for source/build hashes. No optional Git commit hook was run and no commit was created.

## Phases 17–18 — 2026-09-12

Implemented T126–T132 and T134–T142: maintained full-day milestone evidence, post-settlement permanent Level 2, target/reason display, strict v1 save envelopes and cross-record validation, serialized detached saves, native atomic IndexedDB replacement, validation-before-confirmation loads, generation-safe paused runner replacement and explicit Save/Load/New Game feedback. Presentation remounting disposes earlier views and reuses one active frame owner. New named production functions and methods have descriptive comments. Phase 19 is unstarted; no optional commit hook or commit was run.

Final pinned **Node 24.20.0** verification passes **338 tests in 115 files**, strict domain/browser/test types, 24 architectural regression fixtures, descriptive comments on 413 named production functions/methods, both production builds, and shared-graph/settings/output isolation. `npm run check:release` succeeded; the strengthened permanent-award regression also passed its focused run. `git diff --check` is clean.

The complete default-content command journey earns Level 2 at tick 172800 with 96 actual office arrivals, 40 actual restaurant admissions, 272 daily transport samples, quality 94.36158088235292, operating net 56000 minor units and settled cash 355396. A disclosed six-office two-rush stress scenario independently fails/passes the daily quality target with one/two shafts and all other criteria met. Shared metric coefficients, default departure schedules, eight-person capacity and canonical congestion numerical outcomes remain unchanged. The canonical fixture hash is now `2391e0ba7b4679f780e58a900b666418c26eaf8a54436f4409c53d14c516c736`; required progression fields and explicit `tower-mvp-v1` / `mvp-v1` compatibility replace the earlier restaurant identifiers. Old snapshots reject instead of acquiring invented evidence.

Automated tests cover each independent milestone predicate; temporary minima losses/repair; full-day and repeated awards; strict shapes, counters, historical/live ownership and completed-day references; native adapter request-versus-transaction event ordering; detached snapshots, failures/retries, continued play, paused edit races, cancellation and stale reads/writes/callbacks. The complete active-state matrix restores through the end of the next full calendar day. Three seeds across three elapsed days, 30/60/120 presentation schedules, all speeds, pauses and persisted splits produce exact canonical states. The smaller mixed replay, default awarded journey and 2000-person reference are explicitly distinct workloads; see [determinism](acceptance/determinism.md) and [phase guide](progression-and-saves.md).

**T133/T143/T144 remain unchecked.** Initial prepared observations covered Firefox's midnight award and rollback, plus Safari reference Save 913.0 ms / Load 919.0 ms. After the user manually unlocked the Mac, both browsers verified the final release artifact: same-origin saved-state handoff, test-tab close/reopen, paused restoration at the saved tick, and short active traffic continuation. Firefox retained Level 2 and its original award tick across reopen. Final prepared reference timings in Firefox were Save **1130.0 ms** and Load **1173.0 ms**; Safari's real aborted replacement preserved reference tick 29160, which subsequently loaded in release. Artifact hashes were checked against the validated outputs. No source changes or new automated test run were needed for this verification-only follow-up. Chrome remains absent from the enabled inventory; the full ordinary MVP journey, actual Save/Load controlled comparison and complete native next-day trace remain open. The earlier lock is resolved.

Final verification is recorded with source and output hashes in [phase-17-18-artifacts.json](phase-17-18-artifacts.json). The release and prepared builds remain separate; fixture-only native abort and timing tools are excluded from release.

Exact browser observations and remaining procedures are in [C7](../tests/browser/evidence/us15-management-loop.md), [US16 storage](../tests/browser/evidence/us16-storage.md) and [M6](../tests/browser/evidence/full-mvp-loop.md). Native unavailable/quota/blocked-upgrade paths, final performance distributions, sustained sessions and five-new-player evaluation are not inferred from mocked adapter or headless tests.

## Phase 19 — final audit, 2026-09-12

**Release remains blocked.** Gameplay is implemented, but the 2,000-person reference fails simulation/edit budgets and required complete platform/player evaluations remain unmet. T145/T146/T149/T150/T153/T154 are complete; T147/T148/T151/T152 stay unchecked. Earlier native checkpoints remain unchecked. The user confirmed no new-player participants/results are available. No commit, publication or deployment was performed.

Implemented the complete ordinary onboarding journey, coherent toolbar shortcuts and Escape cancellation, native dialog focus protection, keyboard inspector/report navigation and office selection, read-only development diagnostics, bounded native performance recording and valid active/dormant scale scenarios. The build checker now explicitly resolves production environment flags and proves diagnostic code is excluded from emitted output. Existing default balance, capacity, schedule and compatibility identifiers are unchanged.

The final [acceptance map](acceptance-audit.md) inventories all **16 stories, 72 story scenarios, 5 mandatory congestion clauses, every Edge Case, FR-001–057 and SC-001–014**, linking concrete automated evidence and naming unmet native/player/budget gates. Test mappings are evidence coverage, not a claim that an unperformed native scenario passed.

Performance measurement and remediation are in [headless results](performance/headless-results.md), [JSON](performance/headless-results.json), [native probes](performance/browser-results.json) and [remediation](performance/remediation.md). Final benchmarks pass their deterministic assertions (29 tests / 11 files, 31 workload reports; three warmups/ten restored trials), while actual reference median/p95 results **867.09/890.22 ms per 30 rush ticks** and **5669.44/5714.08 ms per active edit** fail release budgets. All shared before/after fixture and final authoritative hashes agree. The native pacing fix yields between completed ticks; it does not pretend that lost simulation throughput is recovered.

The ordinary in-app browser journey built from an empty site, observed a 96-worker overloaded morning, added an actual second shaft, earned Level 2 at tick 172800, and restored the award/current traffic through Save, refresh and Load. This implementer run predates the last pacing change and is labeled supplemental. Native Safari 26.6.2 and Firefox 155.0.1 performed scoped release UI/storage checks; Firefox also exposed poor reference performance in a short prepared diagnostic probe. These are not five-new-player, full-duration or three-browser qualifications. [UI observations](../tests/browser/evidence/ui-polish.md), [static/server-unavailable observations](../tests/browser/evidence/static-offline.md), [sustained-session gate](../tests/browser/evidence/sustained-session.md), [player gate](../tests/browser/evidence/new-player-evaluation.md) and [performance gate](../tests/browser/evidence/performance.md) retain exact limits.

Final command results and source/output hashes are retained in `docs/phase-19-artifacts.json`. `npm run check:release` includes all unfiltered unit/integration tests and the non-skippable congestion gate before strict types, architecture/comments, both builds and isolation checks. The repository README now provides runnable install, validation, separate fixture serving, same-origin native save handoff and root/subdirectory static hosting steps.

Final verification passed on Node 24.20.0: **350 tests in 119 files**, including the required congestion loop; strict types; 24 boundary regression fixtures; descriptive comments for 425 named production functions/methods; release and prepared production builds; graph/settings/output isolation and exclusion of development diagnostics. `git diff --check` passed. The final prepared artifact was observed in the post-pacing Firefox probe (median 42.659 FPS, minimum 34.826; normal throughput still below target). Earlier ordinary journey, static-copy and pre-fix native evidence retain their earlier artifact identity and are not promoted to complete final-release qualification. T154 is complete as an audit with explicit unmet gates; it does not clear the release.

Final-release native smoke in Firefox also passed after refresh: the existing floor save restored seed `9ec24b4bebb2ef6e02b2910750954ba5`, tick 21600 and cash 997600 minor units paused; normal time advanced to 06:38 and paused again. This small empty-workforce check verifies the final release asset loads and retains the native save, without claiming the unperformed complete journey or reference storage budgets.

## Short-trip routing adjustment

The user amended FR-017 after Phase 19: new one-/two-floor trips choose the shortest walk to a usable stair or elevator entrance; equal walking approaches use estimated total cost, then stable route ties. Longer trips still prefer an elevator-containing route and fall back to stairs only when no such route exists. Same-floor walking and committed physical legs remain unchanged. Existing saved short journeys with the legacy `stairs` preference still validate and continue without rewriting active trips; new short journeys persist `nearest`.

The route-mode regressions cover nearer stairs/elevators, upward/downward one-/two-floor travel, large wait estimates, unusable nearby elevators and long-trip fallback. Save continuation covers both short-trip preference values. The restaurant stair fixture now places its stair entrance nearer than the elevator so it continues to test actual stair travel under the amended rule. Previous Phase 19 browser/performance artifact hashes remain historical; this adjustment does not clear those release gates.

Routing-adjustment verification: `npm run check:release` passed **358 tests in 120 files**, including the unchanged mandatory congestion comparison and both short-route save-continuation cases. Strict types, 24 boundary fixtures, descriptive function comments, both production builds and release isolation passed. `git diff --check` is clean. Final routing-build hashes are in `docs/short-route-artifacts.json`; native/performance qualification has not been rerun for this adjustment.
