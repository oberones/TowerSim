# Checkpoint evidence template

- Date, observer and checkpoint:
- Artifact: release dist/ or browser-test dist-browser-test/:
- Commit, dirty diff/archive identity, lockfile and output SHA-256 hashes:
- Node/tool versions and production build commands:
- Browser exact version, OS, machine/CPU/RAM, viewport/DPR and profile:
- Origin and server command:
- Seed, fixture hash, content/rules/schema identity and starting tick:
- Ordinary controls/actions and expected outcomes:
- Actual observations, console/network errors and evidence attachments:
- Automated checks (separate from native observations):
- Same-origin save handoff (when applicable):
- Unmet checks and reason:
- Result: UNVERIFIED until observations are recorded.
