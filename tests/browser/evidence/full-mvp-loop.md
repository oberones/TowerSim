# M6 — complete native MVP journey

Status: **open; T144 is not complete**.

Automated evidence now covers the default construction-to-Level-2 journey, full-population controlled congestion, shared restaurant transportation, daily finances and complete-state next-day continuation. See `tests/integration/mvp-game-loop.test.ts`, `congestion-loop.test.ts`, `save-continuation.test.ts`, `progression-continuation.test.ts` and `three-day-replay.test.ts`.

Scoped native evidence is recorded separately in `us15-management-loop.md` and `us16-storage.md`: Firefox observed the prepared award, saved it across a refresh, canceled replacement and recovered its prior slot after native transaction abort. Safari measured a prepared 2000-person reference Save/Load below two seconds. Neither is the full ordinary construction-to-award-to-refresh-to-continuation journey on the release artifact.

After manual unlock, Safari and Firefox verified final-release saved-state handoff, test-tab reopen and short continued traffic; see `us16-storage.md` for exact states and artifacts. The full three-browser M6 walkthrough, actual Save/Load controlled comparison and complete continuing native day remain unperformed. Chrome is unavailable in the enabled control inventory; the earlier Mac lock is resolved. Five-new-player, sustained-session and final-scale performance acceptance remain phase-19 work and are not implied by these results.

Use the current quickstart's complete walkthrough, record browser/build/hardware and start/end times, save during active traffic, refresh/reopen at the same origin/profile, restore paused, resume and finish the next day. Repeat the fixed-demand congestion baseline by actually loading its saved pre-rush state before the ordinary shaft-B intervention. Keep native observations separate from headless equality checks.
