# Reference browser performance — BLOCKED

T151 remains unchecked. The prepared fixture and bounded observer exist in `tests/browser/scenarios/performance.ts`, selectable as **Performance reference — recorded frames and input**. The observer counts actual delivered application draws and completed ticks, records speed/generation windows, visibility interruptions, action-to-draw samples and native storage status latency, and stops after ten minutes. Copy the visible JSON after stopping; do not interpret a prepared build as release evidence.

A short actual Firefox 155.0.1 probe was performed on Apple M2 Pro/32 GiB at the available 2560×1207 CSS viewport, DPR 1. Seed `00000001000000020000000300000004`, 2,000 people/1,920 initially active. The 76.912-second normal-speed probe produced 56 windows, median 1.495 delivered FPS, median 47.619 ticks/real second and 5504.04 ticks of outstanding debt. None of the normal windows met 30 FPS. No visibility interruptions occurred. One 6 ms start-action observation is not response-budget qualification. Full summary is in `docs/performance/browser-results.json`; the exact observed pre-pacing-fix artifact is identified by `docs/phase-19-observation-artifacts.json`.

The probe exposed 32-tick batches preventing the pacing budget from yielding promptly. `src/app/game/session.ts` now checks the budget after each completed tick while preserving debt and ordered simulation. The failing-before regression is in `tests/unit/pacing.test.ts`; the follow-up is recorded in the JSON. This changes responsiveness, not the core reference-throughput limitation.

The complete 1440×900, ten-minute root release exercise, all speed modes, p95 action/access responses, every save/load ≤2 seconds, and matching artifact observations in current stable Chrome/Firefox/Safari remain required. Chrome is absent from the enabled inventory. Headless reference rush and active-edit failures independently keep the release blocked. No stress population or capacity was reduced to claim success.

After the pacing fix, a 48.474-second Firefox probe of the same initial reference produced 42 normal windows: median 42.659 FPS, p95 44.910 FPS, minimum 34.826 FPS and 100% of sampled normal windows above 30 FPS. Median throughput remained 43.011 ticks/second, with 3293.76 ticks of debt in the final normal window. Start/Pause action-to-draw samples were 5/3 ms. There were no visibility interruptions or observer overflow. This used the final prepared artifact in `docs/phase-19-artifacts.json`. The shorter trace has a different end tick and cannot be used as a paired simulation-throughput comparison. Its frame responsiveness improved, while normal simulation throughput still fails.
